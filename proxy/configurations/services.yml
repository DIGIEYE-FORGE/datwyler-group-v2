http:
  services:
    frontend:
      loadbalancer:
        servers:
          - url: "http://frontend:8084"
   
    backend:
      loadbalancer:
        servers:
          - url: "http://backend:3000"

    auth:
      loadbalancer:
        servers:
          - url: "http://auth:5000"
